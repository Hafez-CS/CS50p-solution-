def main():
    n = (input("Greeting: ")).lower() # get inpute of user with lower word
    print(f"${value(n)}") # print it






def value(a):
    if a.startswith("hello"): # check it
        return 0
    elif a.startswith("h"): # check it
        return 20
    else:
        return 100








if __name__ == "__main__":
    main()